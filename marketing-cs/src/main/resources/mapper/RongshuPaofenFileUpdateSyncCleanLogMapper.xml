<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.clean.rongshu.RongshuPaofenFileUpdateSyncCleanLogMapper">

    <select id="getSyncApicodeId" resultType="java.lang.Long">
        select sync_apicode_id from b_rongshu_paofen_file_update_sync_clean_log where
        api_code='${apiCode}'
        and marketing_clean_data_file_id = ${cleanDataFileId} and sync_apicode_id in
        <foreach collection="syncApicodeIdSet" open="(" close=")" separator="," item="syncId">
            ${syncId}
        </foreach>
        and is_del=0
    </select>
</mapper>