<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.XieChengCollidingDataHitRequestNoInitMapper">
    <select id="selectData" resultType="com.br.marketing.entity.XieChengCollidingDataHitRequestNoInit">
        select id, cell_sha256_code_list
        from b_xiecheng_colliding_data_hit_request_no_init
        where is_delete = 0 and retry_count = 0 and query_status = 0
        <if test="minId != null and minId>0">
            and id > #{minId}
        </if>
        order by id limit #{pageSize};
    </select>

    <update id="updateBatchByIdOfRetryCount" parameterType="java.util.List">
        update b_xiecheng_colliding_data_hit_request_no_init set retry_count = retry_count + 1,update_time = now()
        ,push_time = now(), query_status = 2
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
</mapper>