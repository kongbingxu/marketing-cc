<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.BizTrackingTemplateEdgeMapper">

    <!-- 批量插入 -->
    <insert id="batchInsert">
        INSERT INTO biz_tracking_template_edge (template_id, from_node_id, to_node_id, edge_type, description, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.templateId,jdbcType=VARCHAR}, #{item.fromNodeId,jdbcType=BIGINT}, #{item.toNodeId,jdbcType=BIGINT},
             #{item.edgeType,jdbcType=VARCHAR}, #{item.description,jdbcType=VARCHAR},
             #{item.createTime,jdbcType=TIMESTAMP}, #{item.updateTime,jdbcType=TIMESTAMP})
        </foreach>
    </insert>

    <!-- 根据模板ID查询边详情 -->
    <select id="selectEdgeDetailsByTemplateId" resultType="com.br.marketing.dto.datamap.template.TemplateEdgeDetailVO">
        SELECT
            id,
            from_node_id AS fromNodeId,
            to_node_id AS toNodeId,
            edge_type AS edgeType,
            description
        FROM biz_tracking_template_edge
        WHERE template_id = #{templateId}
        ORDER BY id ASC
    </select>

    <!-- 根据模板ID删除边 -->
    <delete id="deleteByTemplateId">
        DELETE FROM biz_tracking_template_edge WHERE template_id = #{templateId}
    </delete>

</mapper>
