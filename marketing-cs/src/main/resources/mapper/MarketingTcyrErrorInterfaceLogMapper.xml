<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.MarketingTcyrErrorInterfaceLogMapper" >


    <select id="selectNoDealList" resultType="com.br.marketing.entity.MarketingTcyrErrorInterfaceLog">
        SELECT id, api_code, batch_no, sync_file_id, element_count, deal_status, is_del, create_time,
               update_time, request_param, push_result
        FROM b_marketing_tcyr_error_interface_log
        WHERE
            api_code=#{apiCode}
          AND deal_status in(0,4)
        LIMIT #{searchSize}
    </select>

    <update id="updateDealStatus">
        UPDATE
            b_marketing_tcyr_error_interface_log
        SET deal_status = #{dealStatus}
        WHERE id=#{id}
    </update>

    <update id="batchUpdateDealStatus">
        UPDATE
            b_marketing_tcyr_error_interface_log
        SET deal_status = #{dealStatus}
        WHERE  id IN (
        <foreach collection="idList" item="id" index="index" separator=",">
            #{id}
        </foreach>
        )
    </update>
</mapper>