<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.MarketingTaskExtendMapper" >
    <select id="getProducts" resultType="com.br.marketing.vo.TaskInfoVO" parameterType="java.util.List">
        select bmt.batch_number as batchNumber,bmt.task_type as taskType,bmte.task_id as taskId
             ,bmte.rule_id as ruleId,bmte.extend_show_title as extendShowTitle
             ,bmte.strategy_product_json as strategyProductJson
        from b_marketing_task bmt
        left join b_marketing_task_extend bmte on bmt.id = bmte.task_id and bmte.is_del=1
        where bmt.batch_number in
        <foreach collection="batchNumbers" item="batchNumber" separator="," open="(" close=")">
            #{batchNumber}
        </foreach>
    </select>
</mapper>