<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.br.marketing.mapper.MarketingJsonNodeParseMapper">

    <select id="getOriginalData" resultType="java.util.Map">
        select api_code,json_data
        from ${tableName}
        where id  = #{id}
    </select>
</mapper>