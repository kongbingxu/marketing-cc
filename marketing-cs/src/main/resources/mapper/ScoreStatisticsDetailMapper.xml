<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.br.marketing.mapper.ScoreStatisticsDetailMapper">
    <insert id="insertBatch">
        INSERT INTO b_score_statistics_detail (
        statistics_id,
        field_x_value,
        field_y_value,
        field_num,
        create_time,
        update_time
        )values
        <foreach collection="list" item="data" separator=",">
            (#{data.statisticsId},
            #{data.fieldXValue},
            #{data.fieldYValue},
            #{data.fieldNum},
            #{data.createTime},
            #{data.updateTime})
        </foreach>

    </insert>
</mapper>