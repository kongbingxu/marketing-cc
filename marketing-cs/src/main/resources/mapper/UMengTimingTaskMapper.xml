<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.UMengTimingTaskMapper" >

    <select id="getTodayLastTask" resultType="com.br.marketing.entity.UMengTimingTask">
        select id, local_id, api_code, umeng_task_id, task_name, extend, is_del, `status`, create_time, update_time
        from b_umeng_timing_task
        where
            local_id =#{localId}
            and api_code =#{apiCode}
            and status = 1
            and is_del = 1
        order by create_time desc limit 1
    </select>
    <select id="getDataByTaskId" resultType="com.br.marketing.entity.UMengTimingTask">
        SELECT id, local_id, api_code, umeng_task_id, task_name, extend, is_del, `status`, create_time, update_time
        FROM b_umeng_timing_task
        WHERE
            status = 1
            AND is_del = 1
            AND umeng_task_id = #{taskId}
        ORDER BY create_time DESC
        LIMIT 1
    </select>
</mapper>