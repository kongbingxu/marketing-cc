<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.DingDingTableSyncMapper">
    
    <!-- 删除表中所有数据 -->
    <delete id="deleteAll">
        DELETE FROM ${tableName}
    </delete>
    
    <!-- 批量插入数据 -->
    <insert id="batchInsertByValues">
        INSERT INTO ${tableName} (
            <foreach collection="fieldNames" item="fieldName" separator=",">
                ${fieldName}
            </foreach>
        ) VALUES
        <foreach collection="valuesList" item="values" separator=",">
            (
                <foreach collection="values" item="value" separator=",">
                    #{value}
                </foreach>
            )
        </foreach>
    </insert>
    
    <!-- 查询表建表语句 -->
    <select id="getCreateTableSql" resultType="java.util.HashMap">
        SHOW CREATE TABLE ${tableName}
    </select>

</mapper>

