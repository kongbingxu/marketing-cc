<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.DdDataLineCostPriceMapper" >

    <select id="selectList" resultType="com.br.marketing.entity.DdDataLineCostPrice">
        SELECT
            id, line_supplier, project_name, caller, effect_date, is_calc_cost, price, created_by,
            created_time, last_modified_by, last_modified_time, last_modified_user_id, last_modified_user_name
        FROM b_dd_data_line_cost_price
        WHERE
            id > #{searchId}
        ORDER BY id asc
        LIMIT #{searchSize}
    </select>
</mapper>