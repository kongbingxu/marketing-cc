<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.XiechengCollidingDataEliminationMapper">
    <insert id="batchSave" parameterType="java.util.List">
        insert into b_xiecheng_colliding_data_elimination (biz_id, biz_type, cell_sha256_code_list, remark)
        VALUES
        <foreach collection="eliminations" item="item" separator=",">
            (#{item.bizId,jdbcType=BIGINT},
             #{item.bizType,jdbcType=INTEGER},
             #{item.cellSha256CodeList,jdbcType=VARCHAR},
             #{item.remark,jdbcType=VARCHAR})
        </foreach>
    </insert>

    <select id="getExcludeData" resultType="java.lang.String">
        select cell_sha256_code_list
        from b_xiecheng_colliding_data_elimination
        where cell_sha256_code_list in
        <foreach collection="sha256CodeList" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>
</mapper>