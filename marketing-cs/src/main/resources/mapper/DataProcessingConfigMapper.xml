<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.dataProcess.DataProcessingConfigMapper">
    <select id="selectByShardOrderByPriorityLevel" resultType="com.br.marketing.entity.dataProcess.DataProcessingConfig">
        select *
        from b_marketing_data_processing_config
                where is_deleted = 0
                  and mod(crc32(file_type -> '$.fileType'), #{shardingTotalCount}) in
        <foreach collection='shardingItems' item='sharding' open='(' close=')' separator=','>
            #{sharding}
        </foreach>
        order by case when priority_level is not null then 1 else 2 end,priority_level;
    </select>
</mapper>