<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.br.marketing.mapper.shunfeng.ShunfengCompanyDataMapper">

    <update id="updateDataStatus">
        update b_shunfeng_company_data
        set push_status=#{status}
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>


    </update>

    <select id="getCompanyNameList" resultType="com.br.marketing.entity.ShunfengCompanyData">
        select
        *
        from b_shunfeng_company_data
        where local_id = #{localId}
        and status =1 and push_status =1
        <if test="indexId != null and indexId>0">
            and id > #{indexId}
        </if>
        order by id asc limit 2000;

    </select>
</mapper>