<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.br.marketing.mapper.WubaSubmitConversionDataLogMapper" >
    <insert id="batchAdd">
        INSERT INTO b_wuba_submit_conversion_data_log
        (api_code, data_id, cell, batch_no, submit_result)
        VALUES
        <foreach collection ="list" item="item" separator =",">
            (#{item.apiCode}, #{item.dataId}, #{item.cell}, #{item.batchNo}, #{item.submitResult})
        </foreach>
    </insert>
</mapper>